{% raw %}apiVersion: v1
kind: Service
metadata:
  name: {{ include "{% endraw %}example-enhancer-chart{% raw %}.fullname" . }}
  labels:
    {{- include "{% endraw %}example-enhancer-chart{% raw %}.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "{% endraw %}example-enhancer-chart{% raw %}.selectorLabels" . | nindent 4 }}{% endraw %}